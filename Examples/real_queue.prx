%F real_queue.c
%X -n push
%X -n pop
%%
VAR {
    buffer,
    head,
    tail,
    count
}

%P
active [2] proctype p_push() {
#include "_modex_push.pml"
}

active [2] proctype p_pop() {
#include "_modex_pop.pml"
}
